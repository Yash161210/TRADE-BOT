services:
  - type: web
    name: yash-nse-trading-ai
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    region: oregon

